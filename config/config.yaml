# Evaluation configuration
evaluation:
  metrics: ['mae', 'rmse', 'r2', 'mape']
  feature_importance:
    enable_shap: true
    enable_permutation: true
    max_features_plot: 20
  outputs_dir: 'models/evaluation'